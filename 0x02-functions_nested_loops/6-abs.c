#include "main.h"

/**
 * _abs - returns the absolute value of a number
 *
 * @n: number to be checked
 *
 * Return: absolute value of param n
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
